/**
 * Ядро Outbox-фреймворка.
 *
 * <p>Пакет размечен {@link org.jspecify.annotations.NullMarked}: все типы
 * считаются non-null по умолчанию, nullable помечается явно через
 * {@code @Nullable}. Spring Boot 4.1 использует ту же разметку, поэтому
 * NullAway/IDE увидят несовместимость на этапе компиляции.
 */
@NullMarked
package com.company.outbox.core;

import org.jspecify.annotations.NullMarked;
