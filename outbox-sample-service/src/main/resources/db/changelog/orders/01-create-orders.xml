<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-5.0.xsd">

    <changeSet id="001-create-orders" author="dev">

        <createTable tableName="orders">
            <column name="id" type="uuid">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="customer_id" type="uuid">
                <constraints nullable="false"/>
            </column>
            <column name="total" type="numeric(19,2)">
                <constraints nullable="false"/>
            </column>
            <column name="status" type="varchar(20)">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="timestamptz">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <sql>
            ALTER TABLE orders
            ADD CONSTRAINT chk_orders_status
            CHECK (status IN ('NEW', 'COMPLETED', 'CANCELLED'));
        </sql>

        <createIndex indexName="idx_orders_customer_id" tableName="orders">
            <column name="customer_id"/>
        </createIndex>

        <createIndex indexName="idx_orders_status" tableName="orders">
            <column name="status"/>
        </createIndex>

        <rollback>
            <sql>ALTER TABLE orders DROP CONSTRAINT IF EXISTS chk_orders_status;</sql>
            <dropTable tableName="orders"/>
        </rollback>

    </changeSet>

</databaseChangeLog>